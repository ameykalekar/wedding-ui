import { GenericsearchPipe } from './genericsearch.pipe';

describe('GenericsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GenericsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
